# LoRA SFT 微调配置 (LLaMA-Factory)
# 对应 scripts/train.sh

### 模型配置
model_name_or_path: deepseek-ai/DeepSeek-R1-Distill-Qwen-7B
stage: sft
do_train: true
finetuning_type: lora

### LoRA 参数
lora_rank: 8
lora_alpha: 16
lora_dropout: 0.05
lora_target: all

### 数据集配置
dataset: law_sft_train
dataset_dir: data/processed/splits
template: deepseek3
cutoff_len: 4096
val_size: 0.2

### 训练参数
learning_rate: 5.0e-5
num_train_epochs: 3
per_device_train_batch_size: 2
gradient_accumulation_steps: 8
lr_scheduler_type: cosine
warmup_ratio: 0.1

### 日志与保存
logging_steps: 10
save_steps: 100
save_total_limit: 3
eval_strategy: steps
eval_steps: 200
plot_loss: true
report_to: tensorboard

### 精度与设备
bf16: true
load_best_model_at_end: true

### 输出
output_dir: outputs/sft_lora
